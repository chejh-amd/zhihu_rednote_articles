标题：A卡跑AI只能聊天？这次让它直接出图

正文：
前面有人问：A卡装好Qwen，除了聊天还能干什么？
这次不堆参数，直接让它出图。
先说结果：SDXL Base默认工作流顺利跑通，GPU利用率和显存都有明显变化，A卡确实在干活。效果图放在结尾。
ROCm 10扩展了对C o m f y U I生图和视频生成的优化，其中重点调优了SDXL Base、FLUX.2 KLEIN和Wan 2.2。这次先用SDXL Base，看看A卡本地生图的实际体验。

🧩 谁在干活

C o m f y U I负责搭流程，SDXL Base负责画图，ROCm 10负责调动AMD显卡算力。

🔍 先认卡

跑之前先确认识别到GPU。能出图不等于用了显卡，如果实际走的是CPU，生成速度就没有参考意义。

📦 加载SDXL Base

模型放好后直接选中，其他设置先保持默认。不装额外插件，也不改工作流，先把基础出图流程跑通。

🎨 开始生成

填好提示词，按默认设置生成一张1024×1024图片。

提交后进度条开始推进，GPU利用率和显存占用同步变化，可以直观看到A卡正在参与计算。

⚠️ 遇到问题先看这三处

模型列表找不到：检查目录后刷新。

生成特别慢：确认是否跑在CPU。

显存不够：降低分辨率或生成数量。

想复现同款效果，底下扣「提示词」。

你现在用的是哪张AMD显卡？下次想看FLUX、Wan 2.2，还是继续测SDXL？👇一起聊聊。

我们来自AMD个人工程师团队，继续记录AMD显卡本地跑模型的真实过程。